import React from 'react'
import {Button, Container} from 'react-bootstrap'

const Admin = () => {
    return (
        <Container className="d-flex flex-column mt-2 w-5">
            <Button variant={'outline-dark'}>Добавить запрос</Button>
        </Container>
    )
}

export default Admin
